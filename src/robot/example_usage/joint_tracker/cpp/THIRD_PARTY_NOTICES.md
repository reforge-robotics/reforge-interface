# Third-Party Notices

No third-party source code is vendored in this example. The checked-in plotting
adapter is Reforge-owned code that invokes the user's Python Matplotlib
installation in a separate process.

Matplotlib remains subject to its own license; install it from the distribution
package or Python environment used to run the example. The Reforge SDK library
has no plotting dependency.
