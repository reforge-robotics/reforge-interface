#!/usr/bin/env python3
"""Repository-local executable wrapper for the read-only SDK state probe."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))

from robot.standard_bots_control_state_probe import main

raise SystemExit(main())
